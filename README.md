Use this for very large packages like Qt.

Usage:

When ```widgets.go``` is in the same folder:

  ```package_parser widgets```

generates ```widgets.pkg``` and can be opened with:

  ```package_view widgets```


